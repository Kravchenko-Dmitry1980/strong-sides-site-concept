# QA-отчёт: «Нейро-сотрудники» + навигация + главная

**Дата:** 2026-05-03  
**Окружение:** Windows, репозиторий `c:\Dima\Projects\CURSOR\presentation`  
**Ограничение:** визуальный и интерактивный прогон через IDE browser MCP **недоступен** (ошибка `browserContext.newPage: Protocol error (Target.createTarget): Failed to open a new tab`). Ниже — автоматизируемые проверки + статический разбор кода; **полноценная визуальная верификация** выполняется вручную в локальном браузере.

---

## 1. Результат `npm run build`

| Команда | Результат |
|---------|-----------|
| `npm run build` | **Успех** (Vite 7.3.2, exit code 0) |

Предупреждение Rollup: размер чанка JS > 500 kB (известное для текущей сборки, не ошибка сборки).

---

## 2. Результат `npm run dev`

| Параметр | Значение |
|----------|----------|
| Статус | Сервер **запущен** |
| **Фактический URL** | **http://localhost:3004/** (порты 3000–3003 были заняты; Vite выбрал следующий свободный) |
| HTTP smoke test | `GET /` → **200 OK** (`Invoke-WebRequest`, таймаут 10 с) |

**Важно:** при следующем запуске номер порта может отличаться — смотреть вывод `npm run dev` в терминале.

---

## 3. Проверка в браузере / DevTools (MCP)

| Шаг | Результат |
|-----|-----------|
| Открытие локального URL через browser MCP | **Не выполнено** — среда не создаёт вкладку (ошибка протокола выше) |
| Снимок экрана / ARIA через MCP | **Не выполнено** (зависит от того же контекста) |

**Рекомендуемый ручной сценарий:** открыть URL из п.2 → пройти чеклисты п.5–7.

---

## 4. Консоль и сеть (автоматизация)

| Проверка | Результат |
|----------|-----------|
| `o11y_get-console-messages` (MCP) | **Не вызывалось** (браузерный контекст недоступен) |
| Сетевые запросы страницы (MCP) | **Не вызывалось** |

После ручного открытия: в DevTools → Console — ожидается отсутствие **ошибок** (warnings от React StrictMode / HMR допустимы по политике проекта).

---

## 5. Визуальная верификация секций «Нейро-сотрудники»

Источник: порядок компонентов в `src/neuro-agents/NeuroAgentsPremiumPage.jsx` + заголовки в дочерних компонентах.

| Секция | Ожидаемое в UI | Статус автопроверки |
|--------|----------------|---------------------|
| Hero | `NeuroAgentsHero` — заголовок «Нейро-сотрудники», подзаголовок, CTA, trust-bar, визуал | **Код и порядок OK**; визуал вручную |
| Библиотека агентов | `NeuroAgentLibrary`, `id="neuro-agent-library"` | **Код OK** |
| Business impact / KPI | `NeuroAgentsBusinessImpact` | **Код OK** |
| Демо / видео | `NeuroAgentsDemoGallery`, `id="neuro-demo-gallery"` | **Код OK** |
| Why-us | `NeuroAgentsWhyUs` | **Код OK** |
| Финальный CTA | `NeuroAgentsFinalCta`, `id="neuro-final-cta"` | **Код OK** |

---

## 6. Навигация (проверка по коду + ожидаемое поведение)

Файл `src/components/Header.jsx`, массив `LANDING_NAV`, блок `<details className="header-more">` для «Все разделы презентации».

| Пункт | Механика | Статус |
|-------|----------|--------|
| Главная | `anchor: landing-hero` на главной | **OK** (без изменений реализации) |
| Возможности | `landing-platform` | **OK** |
| Как это работает | `landing-process` | **OK** |
| Интеграции | `landing-integrations` | **OK** |
| Демо | `sectionId: "agents"` → раздел нейро-сотрудников | **OK** (в шапке подпись «Демо», не дословно «Нейро-сотрудники») |
| Контакты | `landing-contacts` | **OK** |
| Все разделы презентации | `moreSections` → кнопки по `sections` из `App.jsx`, в т.ч. «Нейро-сотрудники» | **OK** |

`App.jsx`: для `home` и `agents` включена премиум-оболочка; `onAuditCta` передаётся в секции — главная не менялась в этом QA-проходе.

---

## 7. Главная не сломана (регрессия по коду)

| Проверка | Результат |
|----------|-----------|
| `HeroSection` / `B2BLandingPage` не редактировались в этом задании | **OK** |
| Сборка проекта целиком | **OK** (см. п.1) |

Подтверждение «не сломана визуально» — только **вручную** после открытия главной в браузере.

---

## 8. Адаптивность (по стилям)

Файл `src/neuro-agents/neuro-agents.css`, медиазапросы:

| Режим | Условие | Что меняется (кратко) |
|-------|---------|------------------------|
| Desktop | по умолчанию | Сетки 6 / 5 / 3 колонок impact и т.д. |
| ≤1180px | `@media (max-width: 1180px)` | Библиотека 3 колонки; демо 3; impact в 1 колонку |
| ≤900px | `@media (max-width: 900px)` | Хиро в 1 колонку; библиотека 2; why 2; финальный CTA — одна колонка |
| ≤520px | `@media (max-width: 520px)` | Одноколоночные сетки; trust 1 колонка |

**Визуальная** проверка tablet/mobile — вручную (изменение ширины окна DevTools).

---

## 9. Найденные проблемы

| ID | Описание | Критичность |
|----|----------|-------------|
| B1 | Browser MCP не открывает страницу — нет автоматических скриншотов/console | Среда |
| B2 | Порт dev-сервера не фиксирован (3000 может быть занят) | Операционная |

Ошибок сборки, HTTP 5xx на `/`, сломанных импортов по результатам `npm run build` **не выявлено**.

---

## 10. Внесённые исправления

**Нет.** В рамках верификации код не менялся (условие: правки только при подтверждённой ошибке).

---

## 11. Оставшиеся риски

1. **Визуальные регрессии** (наложение блоков, overflow, шрифты) — без ручного просмотра не гарантируются.  
2. **Консоль в runtime** — редкие предупреждения от `recharts`/React в dev-режиме; стоит просмотреть вручную.  
3. **Навигация «Демо» vs название раздела** — в UI шапки «Демо», в списке всех разделов — «Нейро-сотрудники»; это ожидаемо по текущему `Header`, не баг, если так задумано продуктом.

---

## 12. Чеклист для повторного прогона (вручную)

1. `npm run build` — ожидается success.  
2. `npm run dev` — записать URL из вывода.  
3. Открыть главную → прокрутить хиро/блоки.  
4. Через шапку: Главная, Возможности, Как это работает, Интеграции, Контакты — убедиться, что якоря работают на главной.  
5. «Демо» → страница нейро-сотрудников; проверить 6 секций сверху вниз.  
6. «Все разделы презентации» → «Нейро-сотрудники».  
7. Ширины ~1280 / ~768 / ~390 px — нет горизонтального скролла body, читаемость CTA.  
8. Console — нет красных errors.
