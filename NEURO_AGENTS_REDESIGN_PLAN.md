# План редизайна: раздел «Нейро-сотрудники»

**Дата:** 2026-05-03 (обновлено: референсы подтверждены на диске)  
**Репозиторий:** `c:\Dima\Projects\CURSOR\presentation`  
**Ограничение этапа:** только документ; **код не менялся**.

## Референсы: `references/neuro-agents/`

**Наличие файлов:** подтверждено на локальной ФС (листинг `references/neuro-agents/`). Все перечисленные ниже PNG присутствуют.

| Файл | Назначение |
|------|------------|
| `references/neuro-agents/01_full_reference.png` | Полная склейка страницы: вертикальный порядок секций A→E (см. §2) |
| `references/neuro-agents/02_hero.png` | Хиро: копирайт слева, орбитальная визуализация агентов справа, trust-bar |
| `references/neuro-agents/03_agent_library.png` | Библиотека: шапка + ряд из 6 карточек + нижний баннер роста платформы |
| `references/neuro-agents/04_business_impact.png` | Impact: 4 KPI-карточки, график «с/без» + donut, ROI-калькулятор со слайдерами |
| `references/neuro-agents/05_demo_gallery.png` | Демо: 5 видео-карточек (превью, play, длительность) + «Смотреть все демо» |
| `references/neuro-agents/06_why_us_cta.png` | 4 карточки «почему мы» + широкий финальный CTA с мишенью и кнопками |

**Опционально для регрессии:** `07_current_neuro_agents.png` — в каталоге не обязателен; если появится, использовать для сравнения «до/после».

**Визуальный якорь:** премиальная главная — тёмный фон, медь/янтарь/оранжевь, токены в `src/homepage/landing.css` (`--ld-copper`, `--ld-amber`, `--ld-bg-deep`, оболочка `.site-app--b2b-home` + `.site-content--landing`, шапка `.top-header--b2b`).

---

## 1. Текущая реализация

### 1.1. Где живёт раздел

| Файл | Роль |
|------|------|
| `src/sections/NeuroAgentsSection.jsx` | Единственная «страница» раздела: данные агентов, композиция блоков |
| `src/components/AgentShowcase.jsx` | Сетка карточек + детальная панель (свой локальный `activeId`) |
| `src/components/ModuleShowcaseCard.jsx` | Компактная карточка модуля в сетке |
| `src/components/SectionShell.jsx` | Общая оболочка секции: eyebrow, `h2`, lead + `framer-motion` fade-in |

Стили раздела и связанных паттернов: **`src/index.css`** — классы `.section-shell*`, `.agent-showcase*`, `.module-showcase-grid`, `.module-showcase-card*`, `.agents-highlight`, `.split-layout`, `.selector-list`, `.detail-panel`, `.cta-row` (см. также медиазапросы ~1525+, ~1655+).

### 1.2. Структура UI (как сейчас)

1. `SectionShell`: eyebrow «Продуктовые модули», заголовок «Нейро-сотрудники», lead-текст.  
2. `AgentShowcase`: сетка из 3 колонок + нижняя «деталь» с дублирующими CTA.  
3. Сетка `module-showcase-grid` из `ModuleShowcaseCard` (те же агенты, клик меняет `activeId` **в родителе**).  
4. Блок `agents-highlight` («Фокус пилота»).  
5. `split-layout`: слева список кнопок по модулям, справа `detail-panel` с полями и тремя кнопками CTA.

**Замечание по UX/архитектуре:** в `AgentShowcase` используется **отдельный** `useState` для активной карточки, не синхронизированный с `NeuroAgentsSection`. Клик по карточке в шоукейсе и клик по `ModuleShowcaseCard` могут показывать разный «активный» контент в разных зонах — это технический долг для редизайна.

### 1.3. Модель данных (сейчас)

Массив `agents` **захардкожен** в `NeuroAgentsSection.jsx`. Поля: `id`, `title`, `audience`, `does`, `data`, `result`, `status`. Нет категорий, URL демо/видео, иконок, акцентных цветов.

### 1.4. Связь с `App.jsx` и навигацией

- В `src/App.jsx` раздел зарегистрирован в массиве `sections` с **`id: "agents"`**, заголовок **«Нейро-сотрудники»**, компонент `NeuroAgentsSection`. Переключение: стрелки клавиатуры, футер «Назад/Далее», блок **«Все разделы презентации»** в шапке (`Header` → `moreSections`).
- В `src/components/Header.jsx` пункт лендинг-навигации **«Демо»** (`LANDING_NAV`, `key: "demo"`) ведёт на **`sectionId: "agents"`** — то есть с главной пользователь попадает именно в этот раздел.
- `RouteSelector.jsx` не содержит прямой ссылки на `agents`; упоминание «нейро-сотрудники» только в подсказке для маршрута B2B.

### 1.5. Визуальный контекст относительно «премиальной» главной

- Класс **`site-app--b2b-home`** в `App.jsx` выставляется **только** для `id === "home"`. Раздел агентов рендерится в обычной оболочке: фон/glow и панель `site-content` из **`index.css`** (холодная сине-голубая палитра, другая сетка и бордеры), а не тёплая медь/янтарь из **`landing.css`**.
- Шапка уже использует **`top-header--b2b`** глобально; контентная область для не-home не получает **`site-content--landing`**, поэтому разрыв «шапка премиум / тело раздела — legacy» уже сейчас заметен.

---

## 2. Целевая структура (по фактическим PNG `01`–`06`)

Порядок секций сверять с **`01_full_reference.png`** (полная страница сверху вниз). Детали по кропам:

### A. Hero — `02_hero.png`

- Шапка в стиле премиум-лендинга: логотип «Сильные стороны» / подпись платформы, навигация (Главная, Возможности, …, Демо, Контакты), справа CTA «Запросить аудит команды».
- Слева: оверлайн **«AI-МОДУЛИ ПЛАТФОРМЫ BS-EVOLVE»**, заголовок **«Нейро-сотрудники»**, подзаголовок **«AI-модули для пилота и внедрения»**, абзац ценности.
- CTA: заполненная **«Посмотреть демо»** (play) + outline **«Обсудить внедрение»** (документ).
- Нижняя полоса преимуществ (4 пункта): быстрый пилот, безопасность данных, интеграция CRM/BS-Evolve, масштабируемая библиотека.
- Справа: центральная стеклянная карточка с темой «цифровые сотрудники» + **6 плавающих мини-карточек** агентов (иконка + название: аналитик, продажник, трекер, HR, секретарь, KPI-ассистент) и орбитальные линии — отдельный визуальный блок (CSS/SVG/статичный ассет по сложности).

### B. Библиотека — `03_agent_library.png`

- Заголовок **«Библиотека нейроагентов»**, подзаголовок про выбор готовых агентов и рост библиотеки; справа кнопка **«+ Добавить агента»** (ghost, медная обводка).
- Сетка **6 карточек в ряд** (десктоп): верх — визуал «голова/нейросеть» с **разным цветовым свечением** по агенту; имя; **категория** мелким текстом; 2–3 строки описания задачи; **бейдж статуса** (точка + текст: зелёный «Демо доступно», синий «Готов к пилоту», оранжевый «Скоро»); внизу **«Демо»** с иконкой play.
- На макете перечислены агенты: Нейропродажник, Нейроаналитик, Нейросекретарь, Нейро-HR, Нейротрекер развития, **Нейро-KPI ассистент** (в текущем коде последнего нет — добавить в данные при реализации).
- Под сеткой — полноширинный баннер-текст: платформа растёт, новые агенты добавляются регулярно.

### C. Бизнес-эффект — `04_business_impact.png`

- Заголовок секции: **«Как нейроагенты улучшают показатели компании»**.
- **Колонка KPI:** 4 карточки с иконкой, крупной метрикой и подписью (пример с макета: +32% конверсия, −27% рутина, +18% скорость реакции, +24% прозрачность KPI).
- **Центр:** панель «Динамика эффективности с нейроагентами» — табы периода (Неделя / **Месяц** / Квартал / Год), **две линии** (оранжевая «С нейроагентами», серая «Без»), рядом **donut «Экономия времени»** с центральным процентом (на макете 27%).
- **Справа:** карточка **ROI-калькулятора** («Рассчитайте потенциал»): 3 слайдера (число сотрудников, ч/нед рутины, конверсия %) + крупный итог в ₽/год.

### D. Демо-галерея — `05_demo_gallery.png`

- Заголовок **«Смотрите нейроагентов в действии»**, подзаголовок про демо и интерактивные сценарии.
- **5 карточек** в ряд: превью интерфейса, центральная **кнопка play**, **таймкод** в углу превью (например 5:46), под превью — имя агента + короткий слоган сценария.
- Внизу по центру outline-кнопка **«Смотреть все демо»**.

### E. Почему мы + финальный CTA — `06_why_us_cta.png`

- Заголовок **«Почему компании заказывают нейроагентов у нас»**.
- **4 карточки** в ряд (иконка медь + заголовок + абзац): быстрый пилот; связь с KPI; интеграция CRM и BS-Evolve; масштабируемая библиотека.
- Ниже — **широкий CTA-баннер**: слева визуал (мишень со стрелой), центр — заголовок + подзаголовок про аудит и пилот на неделе, справа primary **«Запросить аудит команды →»** и вторичные ссылки **«Посмотреть демо»** / **«Связаться с нами»** с иконками.

---

## 3. Предложение по модели данных (масштабируемая галерея)

Вынести контент в отдельный модуль (например `src/data/neuroAgents.js` или `src/content/neuroAgents.json` + импорт). Один массив объектов; UI только читает и группирует.

Рекомендуемая форма записи (поля из ТЗ + совместимость с текущими текстами):

```ts
/** Тип иконки: строковый ключ для маппинга на SVG/React-иконки без импорта сырого JSX из JSON */
type NeuroAgentIconType =
  | "sales"
  | "analytics"
  | "secretary"
  | "hr"
  | "mentor"
  | "custom";

type NeuroAgentAccent = "copper" | "amber" | "ember" | "gold" | string; // при необходимости — HEX в конфиге пресетов

/** Соответствует бейджам на `03_agent_library.png` */
type NeuroAgentStatusKind = "demo_available" | "pilot_ready" | "soon" | "concept";

interface NeuroAgent {
  id: string;
  name: string; // было: title
  category: string; // как на макете: «Продажи», «Аналитика», «Операции», «HR и подбор», «Развитие команды», «Управление» и т.д.
  targetAudience: string; // было: audience; для детальной панели / модалки
  task: string; // было: does; на карточке библиотеки = краткое описание (2–3 строки)
  result: string;
  /** Человекочитаемая подпись и/или ключ `statusKind` для цвета точки бейджа */
  status: string;
  statusKind: NeuroAgentStatusKind;
  demoUrl: string | null;
  videoUrl: string | null;
  /** Для галереи `05`: постер превью (URL или ключ ассета) */
  thumbnailUrl?: string | null;
  /** Длительность в секундах или строка «5:46» для отображения */
  videoDuration?: string | null;
  iconType: NeuroAgentIconType;
  accentColor: NeuroAgentAccent;
  /** Опционально: сохранить текущее поле «Какие данные использует» */
  dataSources?: string;
}
```

**Отдельные конфиги секций (не обязательно внутри каждого агента):**

- **`whyUsFeatures`** — массив из 4 элементов для `06` (iconKey, title, body).
- **`businessImpactKpis`** — 4 пункта для левой колонки `04` (iconKey, valueLabel, description).
- **`impactChart`** — серии для линейного графика, активный период по умолчанию, данные donut (процент + подпись).
- **`roiCalculatorDefaults`** — начальные значения слайдеров и формула/мок для отображаемой суммы (на реализации уточнить: честный расчёт vs статичный демо-итог как на PNG).

**Индексация для роста:** при >6 карточек в ряду библиотеки — `sortOrder`, `featured`, `tags[]`; для галереи можно фильтровать `videoUrl != null` или отдельный флаг `showInDemoGallery`.

**Статусы:** маппинг `statusKind` → цвет точки бейджа (зелёный / синий / оранжевый) по `03`.

---

## 4. План реализации (после утверждения плана)

### 4.1. Файлы к изменению

| Файл | Зачем |
|------|--------|
| `src/sections/NeuroAgentsSection.jsx` | Сборка новых подсекций из компонентов; единый `useState` выбранного агента (или URL-параметр позже); импорт данных из `data`. |
| `src/App.jsx` | Расширить условие оболочки: для `agents` (и при желании других «лендинговых» слайдов) применять те же классы, что у главной — например общий флаг `site-app--b2b-shell` + `site-content--landing` для визуального паритета с главной. |
| `src/main.jsx` | При выносе стилей в отдельный файл — добавить `import "./…/neuro-agents.css"` (если не всё остаётся в `landing.css`). |

### 4.2. Файлы к созданию (предложение)

| Файл | Назначение |
|------|------------|
| `src/data/neuroAgents.js` (или `.ts` при появлении TS) | Массив `NEURO_AGENTS` + экспорт типов/констант категорий. |
| `src/neuro-agents/NeuroAgentsPage.jsx` (опционально) | Если секция разрастётся — переименовать/оборачивать текущий `NeuroAgentsSection` или заменить его содержимым страницы. |
| `src/neuro-agents/NeuroAgentsHero.jsx` | Хиро по `02`: текстовый столбец, CTA, trust-bar, визуал орбиты (упрощённый в v1 допустим). |
| `src/neuro-agents/NeuroAgentLibrary.jsx` | Шапка + сетка по `03` + баннер внизу; кнопка «+ Добавить агента» (пока декоративная или ведёт в контакты). |
| `src/neuro-agents/NeuroAgentsBusinessImpact.jsx` | Три колонки по `04`: `ImpactKpiColumn`, `ImpactChartPanel` (табы + `recharts` Line + Donut), `RoiCalculatorCard`. |
| `src/neuro-agents/NeuroAgentsDemoGallery.jsx` | Сетка по `05`: компонент `DemoVideoCard` (thumbnail, play, duration, title, tagline). |
| `src/neuro-agents/NeuroAgentsWhyUsCta.jsx` | Сетка из 4 `FeatureCard` + `FinalCtaBanner` по `06`. |
| `src/neuro-agents/NeuroAgentCard.jsx` | Карточка библиотеки: визуал/заглушка головы, акцент, бейдж `AgentStatusBadge`, строка «Демо». |
| `src/neuro-agents/AgentStatusBadge.jsx` | Точка + текст по `statusKind`. |
| `src/neuro-agents/neuro-agents.css` **или** секции в `landing.css` | Стили с корнем `.neuro-agents` / `.b2b-neuro` для изоляции от legacy `.section-shell` в `index.css`. |

**Компоненты для выноса/замены:** логику из `AgentShowcase` и `ModuleShowcaseCard` **не копировать дословно** — объединить в одну модель выбора и одну карточку; `SectionShell` можно заменить на локальный хедер страницы в стиле лендинга или обернуть первый экран без «холодного» eyebrow из старых токенов.

### 4.3. CSS-стратегия

1. **Переиспользовать токены** из `landing.css` (`:root` с `--ld-*`) для всех новых блоков.  
2. **Не смешивать** старые голубые бордеры `index.css` с новыми блоками: либо отдельный корневой класс страницы агентов с переопределением фона `site-content`, либо условные классы в `App` как для главной.  
3. Сетки: `clamp()` для отступов и `minmax` для карточек — по аналогии с `.b2b-landing` на главной.  
4. Кнопки: классы `.landing-btn`, `.landing-btn--outline` из `landing.css` для согласованности с CTA главной.

### 4.4. Анимации и интерактив

- **`framer-motion`** уже используется в `SectionShell` — применить точечно: stagger для сетки карточек, `layout` при фильтрации, лёгкий `whileHover` на карточках (как в `RouteSelector`).  
- Избегать тяжёлого параллакса на всей странице; при необходимости фона — переиспользовать паттерн с главной (сетка/градиенты), без обязательного Three.js на этой странице.  
- Галерея демо (`05`): hover на карточке (усиление обводки / scale play); клик — модалка с `videoUrl` или заглушка «Скоро».
- Библиотека (`03`): лёгкий hover на карточках; орбитальный хиро — без тяжёлого постоянного WebGL в MVP, если нет готового ассета.
- ROI-блок (`04`): анимация пересчёта при движении слайдеров (тонко, без отвлечения от чтения KPI).

---

## 5. Риски

| Риск | Митигация |
|------|-----------|
| **Визуальная перегрузка** | Иерархия как на `01`: один хиро; в библиотеке — плотная сетка из 6 карточек без лишнего текста; в демо — ряд из 5 превью + одна вторичная CTA; KPI-блок трёхколоночный — на мобильном раскладывать в столбец. |
| **Слишком много хардкода** | Весь контент агентов и секций «почему мы» — в `data` + маленькие константы для KPI/CTA; в JSX только композиция. |
| **Мобильная адаптация и 6 колонок** | Библиотека/демо: на узкой ширине — горизонтальный scroll с `snap`, перенос 2×3 или 1 колонка; KPI + график + ROI — вертикальный стек; видео — постер + полноэкран по тапу. |
| **Масштабирование числа агентов** | Категории + поиск (опционально вторым этапом); `featured` для главной сетки; ленивая подгрузка изображений превью. |
| **Рассинхрон с главной** | Общие токены и общая оболочка `App` для `home` и `agents`; пиксельная сверка с `references/neuro-agents/01_full_reference.png` на ~1440px. |
| **Сложность ROI и графиков** | MVP: визуал как на `04` с фиксированными/моковыми данными; второй этап — осмысленный расчёт ROI и живые данные графика. |
| **Дублирование состояния (наследие)** | При рефакторинге — один источник `selectedAgentId` на странице. |

---

## 6. Критерии приёмки

- [ ] Визуально страница **согласована** с премиальной главной: тёмный фон, медь/янтарь/оранжевые акценты, типографика и кнопки в том же семействе, что `landing.css`.  
- [ ] **Библиотека** (`03`) и **демо-ряд** (`05`) строятся из конфигурации; новая запись в данных даёт карточку в библиотеке; витрина демо может фильтровать подмножество (на макете **5** превью + кнопка «Смотреть все демо»).  
- [ ] **Плейсхолдеры:** нет `videoUrl` / нет постера — заглушка превью + «Скоро» или неактивный play, без битых изображений.  
- [ ] Блок **бизнес-эффекта** читабелен за 5–10 секунд (цифры или понятные метрики + подписи).  
- [ ] Блок **«почему мы»** и **финальный CTA** завершают страницу и ведут к следующему шагу (контакты / пилот — как в продуктовой логике).  
- [ ] **`npm run build`** завершается без ошибок.  
- [ ] Навигация: пункт **«Демо»** в шапке по-прежнему открывает раздел `agents`; при необходимости обновить подпись в `header-more`, если переименуете раздел в `sections`.  
- [ ] Регрессия по **`07_current_neuro_agents.png`** (если добавлен вручную): явное улучшение без потери ключевой информации (аудитория, задача, результат, статус).

---

## 7. Чеклист верификации (референсы на месте)

1. Открыть рядом: `references/neuro-agents/01_full_reference.png` и dev-сборку на ширине **~1440px** — порядок секций A→E как в §2.  
2. Пройти кропы `02`…`06` и отметить отклонения по отступам, типографике и цветам (eyedropper к `--ld-*`).  
3. Проверить, что в данных есть **все агенты с макета** (включая **Нейро-KPI ассистент** для `03`/`05`).  
4. Зафиксировать расхождения копирайта отдельно от задачи вёрстки.  
5. `npm run build` (PowerShell): `npm run build`.
